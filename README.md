# GA1.1-teamOlivine10
GA 1.1

# Function ADD
In the function add you can add up two or more numbers to each other. It might break when python does not know how to combine two different datatypes. For example letters instead of numbers or as given in the next example "3"+ 5. 


Subtract: This notebook can be used to subtract two values from each other. To use it, input values for a and b and run all cells.


Multiply: the multiply function allows a user to enter two variables which are then mutiplied

# Extension Function Subteam B
New code generates the list of sums for each value in the list, sums up every previous value.